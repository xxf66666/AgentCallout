# AgentCallout v0.7.0

2026-09-14。npm 分发就绪与安装链路加固版本。

## 本版功能

- **npm-ready**：`private` 移除、`prepublishOnly` 挂完整 verify、`publishConfig` 锁定官方 registry；`npm pack --dry-run` 清单核对（17 文件）。真实 publish 待 `npm login`（BLOCKERS #0），解除后一条命令完成。
- `REVISION_CONFLICT` 错误现在携带持有者 pid 与锁恢复指引（此前为无上下文的单句）。
- `docs/runbooks.md`：三类故障恢复（修订锁残留 / Windows Sharp EBUSY / install-links 残留）+ Git→npm 迁移步骤。
- `docs/npm-distribution.md`：发布状态、双安装路径、约束（无自动 publish、无 postinstall 下载、无遥测）。

## 证据

- 完整 verify：macOS 本机与 Linux 容器（node:24 ubuntu，**LINUX-GATE-OK**，247 passed + 4 skipped）双环境通过。
- 版本 0.7.0；双标签已推送。

## 已知边界

- Linux 双客户端人工验收需真实 Linux 桌面环境（容器内无法运行 Claude/Codex CLI），保持 NOT VERIFIED 不外推；
- npm publish 后需补记 registry 安装形态证据。
